import { StyleSheet } from "react-native"
import {wp, SCREEN_HEIGHT} from '../../responsive';
const styles = StyleSheet.create({
  container: {
      alignItems:'center',
    justifyContent:'flex-start',
  },
});

export default styles;